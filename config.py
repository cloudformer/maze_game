# config.py —— 外观设置(你可以随便改的地方)
# 想让迷宫长什么样,只改这一个文件就行,别的代码都不用动。
#
# 重要:每个符号都用【2 个字符】宽。
# 因为终端里字符"高"约是"宽"的两倍,每格占 2 个字符,迷宫看起来才是正方形。
# 打印时原样拼出来就对了,程序不再做任何加宽处理。

WALL = "██"    # 墙(两个实心方块,连成一片)
PATH = "  "    # 路(两个空格)
EXIT = "E "    # 出口(走到这里就通关)

# 玩家小人图标清单:第1个给玩家1,第2个给玩家2。每个占 2 个字符宽(多了自动裁成2个)。
# 想加第3个人:这里补一个图标,同时在 game.CONTROLS 里补一套控制键。
PLAYERS = ["**", "()"]

# 看 bot 跑的动画:每走一步停顿多少秒。想快就调小,想慢就调大。
BOT_STEP_DELAY = 0.2


# 预设关卡尺寸:关卡编号 -> 迷宫边长(宽=高)。目前固定 9 关,每关 +5。
# 想调某一关的大小,只改这里对应的数字。
# 小提示:偶数边长(如 10)生成时会自动 +1 变成奇数(11),迷宫算法需要奇数。
MAP_SIZES = {
    1: 5,
    2: 10,
    3: 15,
    4: 20,
    5: 25,
    6: 30,
    7: 35,
    8: 40,
    9: 45,
}
